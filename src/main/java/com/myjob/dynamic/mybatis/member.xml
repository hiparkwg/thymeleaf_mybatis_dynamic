<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper      
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"      
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dynamic">
    <select id="list" parameterType="String" resultType="MemberVo">
      select * from member
      where id=#{_parameter} 
      or    name     like '%${_parameter}%'
      or    address1 like '%{_parameter}%'
      or    phone    like '%{_parameter}%'
    </select>

    <select id="view" parameterType="String" resultType="MemberVo">
        select * from member where id=#{_parameter}
    </select>

    <select id='if' parameterType="Integer" resultType="MemberVo">
        select * from member
        <if test="_parameter>0">
            limit 0,10
        </if>
    </select>

    
</mapper>